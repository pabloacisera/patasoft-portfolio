{
  "name": "patasoft-frontend",
  "version": "1.0.0",
  "description": "PataSoft Frontend - Vite + Vanilla JS",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:local": "vite --config vite.config.dev.js --port 5173",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:report": "playwright show-report"
  },
  "dependencies": {
    "markdown-it": "^14.2.0",
    "socket.io-client": "^4.8.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "jsdom": "^26.1.0",
    "vite": "^5.4.0",
    "vitest": "^4.1.8"
  }
}
